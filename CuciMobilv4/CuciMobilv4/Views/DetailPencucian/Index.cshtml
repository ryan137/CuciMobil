@model IEnumerable<CuciMobilv4.tblDetailPencucian>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tblHarga.NamaJenisPencucian)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblTransaksi.NoKendaraan)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tblHarga.NamaJenisPencucian)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblTransaksi.NoKendaraan)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdDetail }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdDetail }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdDetail })
        </td>
    </tr>
}

</table>
